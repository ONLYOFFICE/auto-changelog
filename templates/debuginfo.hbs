{{#if custom.gatsby}}
---
{{#if custom.title}}title: "{{custom.title}}"{{/if}}
{{#if custom.path}}path: "/{{custom.path}}"{{/if}}
date: {{details.buildDate}}
---
{{/if}}
## Changelog
- `Branch Name:` {{details.currentBranchName}}
- `Hash of last commit:` {{details.hashLastCommit}}

{{#each releases}}
  {{#commit-list groups}}
    ### {{niceDate}}:
      {{#each authors}}
        #### {{author}}: 
          {{#each commits}}
          - {{subject}} {{#if href}}[`{{shorthash}}`]({{href}}){{/if}}
          {{/each}}
      {{/each}}
  {{/commit-list}}

{{/each}}
